<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title%></title>
    <style>
        div.data{
            width:100px;
            height:130px;
            margin-right:10px;
            float:left;
            border:1px solid red;
            cursor: pointer;
        }
        .data div{
            width:100px;
            height:100px;
            background:red;

        }
        .data p{
            margin:5px ;
        }
    </style>
</head>
<body>
    <% for(var i = 0; i < lists.length ; i++){ %>
        <div class='data' onclick="clickItem(this)">
            <div></div>
            <p><%= lists[i] %></p>
        </div>
        <% }%>
    <script src="https://cdn.jsdelivr.net/npm/jquery@1.12.4/dist/jquery.min.js"></script>
    <script>
        function clickItem(_this){
            let p = $(_this).find("p").text();
            console.log(p);
            $.get('/'+p);
        }
    </script>
</body>
</html>